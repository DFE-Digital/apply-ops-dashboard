<% if environment == 'qa' %>
  <% prs = Diff.pull_requests_between('master', state.latest_successfull_build_to('qa').commit_sha) %>

  <% if prs.size == 0 %>
    <div class='govuk-body'>
      👌 All of the PRs on master are deployed
    </div>
  <% else %>
    <div class='govuk-heading-s'><%= prs.size %> PRs behind master</div>

    <div class='govuk-body'>
      The following PRs haven't been deployed to QA yet (<a href='<%= latest_build.diff_against_url('master') %>' class='govuk-!-font-size-19'>see the full diff of QA against master</a>):
    </div>

    <ol class='govuk-list govuk-list--number govuk-body-s'>
      <% prs.each do |author, title| %>
        <li><%= title %> (<%= author %>)</li>
      <% end %>
    </ol>
  <% end %>
<% elsif environment == 'sandbox' %>
  <% prs = Diff.pull_requests_between(state.latest_successfull_build_to('qa').commit_sha, state.latest_successfull_build_to('sandbox').commit_sha) %>

  <% if prs.size == 0 %>
    <div class='govuk-body'>
      👌 Sandbox is up to date with QA
    </div>
  <% else %>
    <div class='govuk-heading-s'><%= prs.size %> PRs behind QA</div>

    <div class='govuk-body'>
      The following PRs haven't been deployed to sandbox yet (<a href='<%= latest_build.diff_against_url(state.latest_successfull_build_to('qa').commit_sha) %>' class='govuk-!-font-size-19'>see the full diff of sandbox against QA</a>):
    </div>

    <ol class='govuk-list govuk-list--number govuk-body-s'>
      <% prs.each do |author, title| %>
        <li><%= title %> (<%= author %>)</li>
      <% end %>
    </ol>

    <div class="govuk-grid-row" id="sandbox-deploy">
      <div class="govuk-grid-column-three-quarters">
        <label class="govuk-visually-hidden" for="sandbox-deploy-input">Commit SHA for the sandbox deploy</label>
        <input id="sandbox-deploy-input" value='<%= state.latest_successfull_build_to('qa').commit_sha %>' class='govuk-input' readonly>
      </div>
      <div class="govuk-grid-column-one-quarter">
        <button data-prevent-double-click="true" class="govuk-button" data-module="govuk-button" onclick="onDeployButtonClick('<%= state.latest_successfull_build_to('qa').commit_sha %>', 'sandbox');" style="width: 100%">
           Deploy 🚢
        </button>
      </div>
    </div>
  <% end %>
<% elsif environment == 'production' %>
  <% prs = Diff.pull_requests_between(state.latest_successfull_build_to('sandbox').commit_sha, state.latest_successfull_build_to('production').commit_sha) %>

  <% if prs.size == 0 %>
    <div class='govuk-body'>
      👌 Production is up to date with Sandbox
    </div>
  <% else %>
    <div class='govuk-heading-s'><%= prs.size %> PRs behind Sandbox</div>

    <div class='govuk-body'>
      The following PRs haven't been deployed to production yet (<a href='<%= latest_build.diff_against_url(state.latest_successfull_build_to('sandbox').commit_sha) %>' class='govuk-!-font-size-19'>see the full diff of Production against Sandbox</a>):
    </div>

    <ol class='govuk-list govuk-list--number govuk-body-s'>
      <% prs.each do |author, title| %>
        <li><%= title %> (<%= author %>)</li>
      <% end %>
    </ol>

    <div class="govuk-grid-row" id="production-deploy">
      <div class="govuk-grid-column-three-quarters">
        <label class="govuk-visually-hidden" for="sandbox-deploy-input">Commit SHA for the production deploy</label>
        <input id="production-deploy-input" value='<%= state.latest_successfull_build_to('sandbox').commit_sha %>' class='govuk-input' readonly>
      </div>
      <div class="govuk-grid-column-one-quarter">
        <button data-prevent-double-click="true" class="govuk-button" data-module="govuk-button" onclick="onDeployButtonClick('<%= state.latest_successfull_build_to('sandbox').commit_sha %>', 'production');" style="width: 100%">
           Deploy 🚢
        </button>
      </div>
    </div>
  <% end %>
<% end %>
